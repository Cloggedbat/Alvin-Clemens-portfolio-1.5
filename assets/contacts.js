


$('.form-control').on('submit', (event) => {
  event.preventDefault();
  let arr = [];
  let err;
  
//   let name = $('#first').val().trim();
//   let note = $('#branch').val().trim();
//   let email = $('#dod').val().trim();
//   // let convertedDate;
  
//   //Convert date to more user friendly format
//   // function dateConversion(date) {
//   //     const d = new Date(date);
//   //     const ye = new Intl.DateTimeFormat('en', { year: 'numeric' }).format(d);
//   //     const mo = new Intl.DateTimeFormat('en', { month: 'short' }).format(d);
//   //     const da = new Intl.DateTimeFormat('en', { day: '2-digit' }).format(d);
//   //     convertedDate = `${da}-${mo}-${ye}`
//   //     return convertedDate
//   // }

//   arr.push(name, note, email);

//   //Confirm that no fields were left empty
//   for(i = 0; i < arr.length; i++) {
//       if(isEmpty(arr[i])) {
//           err = true;
//           $('#error').text('None of the above fields can be left blank');
//       } else {
//           err = false;
//       }
//   };

//   //If there are no errors, data is posted to the admin page for approval
//   if(!err) {
//       const newMember = {
//           name: name,
//           email: email,
//           notes: note,
//       };

      
//   }
});